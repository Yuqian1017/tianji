# 交接文档 — 第二章交付后（2026-07-13）

> 写给下一个 session 的落听潮。本 session 完成了：第二章《装卦》全链（跟随制审计→三 KP 卡→七幕剧本→两轮审查收敛→宗主过目→328 节点数据化→lint→E2E 四轮：optimal 女/错线 B·C·B/师兄版 optimal/含 E2E 抓 bug 修复）。读完本文档再动手。

## 1. 三分钟状态全景

**能玩**：preview 配置 `tianji-dev`（端口 5926）→ `http://localhost:5926/game.html`。标题页：第一章通关后出现「继续主线 · 第二章《装卦》」；番外《钱囊》入口已回迁**第二章通关 + 好感 ≥8**（策划案原定位置）。

**本批交付**（commit `87efa71`→`2060963`+，全部本地未 push）：

| 层 | 内容 | 关键文件 |
|----|------|----------|
| 审计 | 装卦三 KP 原文照录 + 双典籍互证 + 引擎对撞（八宫 64/64·纳甲 24/24·世应 64/64） | `database/sources/ctext/zhuanggua-sanjian-2026-07-12.json` + 快照 ×2 |
| 知识库 | KP-LY-004/005/006 全 accepted（宗主 2026-07-12 委托裁定） | `database/knowledge/kp-ly-00{4,5,6}.json` |
| 剧本（过目已过） | 七幕 ~9.5k 字，3 KP 闭环，3 CP，明账暗账推理层，钱囊暗线只喂料 | `docs/script/CHAPTER_2_SCRIPT_v1.md`（v1.1+3 处转录期修正） |
| 剧本数据 | 328 节点（116+212 双 agent 转录，双层验收，字数保真 +1.7%） | `src/game/chapters/chapter2.js` |
| lint | `npm run audit:chapter2`（图连通/CP/KP gate/账目 37·10/装卦盘↔引擎对撞） | `scripts/validation/audit-chapter2-script.mjs` |
| 引擎扩展 | dressingUpdate 节点（全量幂等盘面）+ DressingBoard（CastPanel 内）+ perThrow.speaker + 无 speaker response 渲染 + 通用化（场景正则/KP_SHORT/SaveSummary/chapterEnd.title） | `src/game/{Player,CastPanel}.jsx` `presentation.js` |
| 入口 | GameModule 第二章按钮 + 番外门槛迁 ch2 | `src/game/GameModule.jsx` |

## 2. 已验证 vs 未验证（诚实清单）

**已验证**（本 session 实测，E2E 均为运行时全程通关）：
- optimal 线（女版）：灵力 74（37+37）/好感 20/三 KP 掌握/choiceLog A·A·A/动态籍贯中孚→「艮宫第七，游魂」
- 错线（B/C/B，两族错误 verdict 全覆盖）：灵力 59（+22 无掌握奖）/好感 15（CP 零好感）/三 KP 待复习停「用过」/不锁进度
- 师兄版 optimal：账目同女版精确；♂ 变体实渲染探针 + 师兄/师弟称谓；动态籍贯贲→「艮宫第二，一世」（两分支全覆盖）
- audit:chapter2 全绿 328/328；eslint 0；vite build exit 0；ch1/qiannang 回归不受影响（registry 纯增量）

**仍未验证（下 session/玩测）**：
1. 装卦盘（DressingBoard）**视觉**细节——状态断言全过，但未截图逐 UI 检查（盘面与中央卡片在矮屏的重叠是已知 P3）
2. BGM 实际听感（ch2 沿用 ch1 曲目 + mystery/ritual 分配）
3. 真人玩测（PRD 终极验收：拿三枚硬币起一卦——ch1 遗留至今）
4. 第二章通关时长实测（估 28-45 分钟，贴下界，装卦交互实耗待真人）

## 3. 下一步待办（优先级）

1. **第三章《六亲》全链**（主战场）：KP 候选＝六亲歌（《增删》六親歌章第五已在快照 950329 提取行 657-683）+ 用神初步？——**认知负荷实测再裁 ≤3**；跟随制审计流程照抄本批（witness 先例全就绪）。剧情钩子：修书房三人立人 + 明夷「更怕见人」的那页 + 好感 20 进「信任」档（25 差一步——注意档位设计）。
2. 立绘表情差分 + 集市背景补图 + 藏经阁内部图（toolbox，`scripts/generate-game-assets.py`）
3. 摇卦/装卦 SFX（freesound 或 ElevenLabs）
4. 玩测批次（真人 + 时长校准 + DressingBoard 视觉）

## 4. 红线（全集，新增两条）

1. 知识正确性：accepted KP 才进剧情；原文照录+定位+互证；**编造出处死罪**。
2. 每章 ≤3 新 KP 且示范→引导→独立闭环；未教不考；好感不参与升境。
3. 铜钱面：背=3（三背=重=老阳）勿回退。
4. 剧本先行：数据改动必须先改剧本 doc（本批 3 处转录期修正即按此流程）。
5. commit 随时、push 要宗主明说；`.claude/launch.json` 既有未提交改动勿动。
6. 美术卦纹装饰不作教学参照；教学卦象一律代码绘制（DressingBoard 属教学层——改它须对齐引擎数据）。
7. **新**：dressingUpdate 节点 board 必须携带全量状态（幂等/刷新恢复契约），勿改成增量。
8. **新**：E2E walker 必须用 MessageChannel 泵（Browser pane 后台 timer 节流会假死 setInterval）。

## 5. 技术备忘（坑与契约）

- resumeCast 契约不变（ch2-s6-130 是第二个使用点）；六掷完成 advance 当前节点 next。
- teachMoment/dressingUpdate 均为静默自结算节点；勿改成停留节点。
- 掌握 +5 统一由 CP reward 发放；ch2 对账基准：教学 12 + CP 15 + 通关 10 = 37，好感 10。
- perThrow.speaker 可选（默认沈疏桐——ch1 兼容）；ch2 案卦全 `{{player}}` 自报。
- chapterEnd 节点可带 title（缺省回退「第一章·终」——qiannang 数据尚未补 title，顺手项）。
- React 18 批处理：程序化连点要间隔——但 setTimeout/setInterval 在 Browser pane 被节流，用 MessageChannel（walker 模式见 CHANGELOG 2026-07-13 工程注记）。
- 动态籍贯文本：`natalPalaceText()`（state.js:121，节点/选项标 `dynamicNatal: true` 触发）——新章若加动态文本走同一 helper。

## 6. 交接 prompt（宗主直接贴给下个 session）

```
项目：天机（修仙恋爱教学游戏），路径 ~/CC projects/projects/games/tianji/

先读 docs/HANDOFF_2026-07-13_ch2.md（交接：状态/红线/技术契约/待办），
再按其指示行动。当前状态：第一、二章 + 番外《钱囊》均可玩且 E2E 通关
（第二章含错线/师兄版补验），番外入口已回迁第二章后，管道全就绪。

本 session 任务：第三章《六亲》全链（交接文档 § 3 第 1 条）：
KP 清单（六亲歌为主口径，witness 950329 已有提取先例，≤3 认知负荷实测再裁）
→ 跟随制审计 → 剧本（写作→/iterative-review 收敛→给我过目）→
过目后数据化+E2E。剧情钩子：修书房三人立人 + 明夷暗账悬念推进 +
好感「信任」档逼近（25）的档位演出。

红线照 HANDOFF § 4 全集执行。
```
