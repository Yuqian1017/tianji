# 交接文档 — M2 阶段（2026-07-12）

> 写给下一个 session 的落听潮。本 session 完成了：第一章剧本 M0 全链（写作→三轮审查→宗主过目通过）→ M1 数据层 → M1 可玩垂直切片 → Galgame 演出层 → M2 策划案。读完本文档再动手。

## 1. 三分钟状态全景

**能玩**：`npm run dev:frontend`（或 preview 配置 tianji-dev，端口 5926）→ `http://localhost:5926/game.html`（独立入口，无 auth 墙）。主 App 也有「天机·第一卦」tab。

**已交付链**（commit `66f1e4f`→`dabad7d`，全部本地未 push）：

| 层 | 内容 | 关键文件 |
|----|------|----------|
| 剧本（M0 已过目） | 八幕 10,729 字，3 KP 闭环，3 计分 CP，三轮审查收敛 | `docs/script/CHAPTER_1_SCRIPT_v1.md` |
| 知识库 | 3 张 accepted KP 卡 + ctext/wikisource 见证快照 | `database/knowledge/` `database/sources/ctext/` |
| 剧本数据 | 320 节点流（转录忠实度双层验收） | `src/game/chapters/chapter1.js` |
| lint | `npm run audit:chapter1`（图连通/CP 完整/KP gate/账目 37·10） | `scripts/validation/audit-chapter1-script.mjs` |
| 播放器 | 9 类节点/摇卦状态机（fixed+interleave+resumeCast+random）/三数值/存档 | `src/game/Player.jsx` `state.js` `CastPanel.jsx` |
| Galgame 层 | 8 背景 + 双性别立绘 + 标题 + 4 BGM；对话框/立绘/BGM 切换 | `src/game/presentation.js` `public/assets/game/` |
| 策划案 | 好感等级/《钱囊》bonus 章/卦阵对弈草案/RPG 元素裁决/第二章预备 | `docs/design/LOVE_AND_SYSTEMS_DESIGN.md` |

## 2. 已验证 vs 未验证（诚实清单）

**已验证**（本 session 实测）：optimal 线全程 E2E 通关（灵力 37/好感 10/三 KP 掌握/本命卦落档/choiceLog）；摇卦中断-恢复状态机精确落点；CP-03 动态选项文本与实际随机卦象吻合；背景精确切换（s1-060 接引殿实证）+ 场景兜底；立绘 multiply 融合（浅景）；标题页/幕间卡/对话框/名牌/HUD；eslint 0；audit:chapter1 全绿；audit:liuyao 17,026 项 0 fail。

**未验证（M2 必做）**：
1. **CP B/C 错线运行时遍历**（lint 图连通 + 转录人工核过，但没在浏览器走过）
2. **师兄版全程通关**（PRD § 9.3 验收⑤）
3. 深色场景立绘鬼影（multiply 代价）+ preload 修复后的全章演出连贯性
4. BGM 实际听感（生成了没细听）+ autoplay 在真实浏览器的表现
5. 玩家现实测试「拿三枚硬币起一卦」（PRD 验收③——终极验收，需真人）

## 3. M2 待办（建议优先级）

1. **玩测修缮**：宗主/真人通关（双性别+错线）→ 修观感问题；立绘 rembg 抠图（`pip install rembg` 或 toolbox 加 client）+ 表情差分；PNG→webp 压缩（~3MB/张太重）
2. **《钱囊》bonus 章**：策划案 § 1.2 已定框架 → 剧本（走第一章同流程：写作→审查→过目）——**量小，是验证 bonus 形态的最佳试点**
3. **第二章《装卦》**：KP 清单先行（策划案 § 4）→ **跟随制审计**（卜筮正宗装卦规则为主口径，排盘引擎已对齐）→ 剧本 → 数据化（复用管道）
4. 摇卦演出打磨（掷钱动画/音效——freesound 或 ElevenLabs SFX）
5. 卦阵对弈原型（策划案 § 2，第三章后才需要，不急）

## 4. 红线（新旧全集）

1. **知识正确性**：进剧情的知识必须 accepted KP（原文照录+定位+互证）；拿不准标 ⚠️ 待核；**编造出处是死罪**。宗主 2026-07-12 再次强调：符合权威典籍 + 权威教材。
2. **美术卦纹边界**：生成美术里的卦纹是装饰，不作教学参照（`public/assets/game/README.md` 已声明）；教学卦象一律代码绘制。
3. **铜钱面口径**：背=3（三背=重=老阳）。engine.js 与 UI 已按典籍修正——新代码勿回退。
4. 剧本先行：新章剧本过目后才进开发；剧本数据化后必须过 `audit:chapter1` 式 lint。
5. commit 随时、push 要宗主明说；`.claude/launch.json`（项目内）有既有未提交改动勿动；全部 commit 目前未 push。
6. 认知负荷 ≤3 KP/章；未教不考；好感不参与升境。
7. 美术音乐用 toolbox（`~/CC projects/_system/toolbox/`，README 30 秒上手；生成脚本 `scripts/generate-game-assets.py` 可复用改造）。

## 5. 技术备忘（坑与契约）

- **resumeCast 契约**：cast 中断链恢复靠节点 `resumeCast: true` 标记（现仅 ch1-s5-290）；六掷完成 advance 当前节点 next。新摇卦交互照此模式。
- **teachMoment 防重复**：processedTm ref 守卫 + 结算即 advance；勿改成停留节点（刷新会重复结算）。
- **掌握 +5 统一由 CP reward 发放**（勿在教学块双计）；对账基准：教学块 12 + CP 15 + 通关 10 = 37。
- React 18 批处理：程序化连点被合并——E2E walker 要 setTimeout 间隔（本 session 的 walker 模式可复用）。
- HMR 会保留 useState——演出层调试要清档硬刷新验证，勿信热更中间态。
- gpt-image-2：竖版立绘 1024x1536、横版 1536x1024、quality medium 够用；ElevenLabs music 60s ~11s 生成。
- 大图必须 preload（GameModule 已接 PRELOAD_IMAGES）。

## 6. 交接 prompt（宗主直接贴给下个 session）

```
项目：天机（修仙恋爱教学游戏，Galgame 形态），路径 ~/CC projects/projects/games/tianji/

先读 docs/HANDOFF_2026-07-12_m2.md（交接文档，含状态全景/红线/技术契约），
再按其指示行动。当前状态：第一章可玩（game.html 独立入口），M0/M1 完成，
Galgame 演出层已上（8 背景+立绘+BGM）。

本 session 任务（按交接文档 § 3 优先级）：
1. 玩测修缮：先自己全程通关一遍（含师兄版 + CP 错线），修观感/演出问题；
   立绘 rembg 抠图去底；PNG 转 webp。
2. 《钱囊》情感 bonus 章剧本（docs/design/LOVE_AND_SYSTEMS_DESIGN.md § 1.2
   已定框架）——走第一章同流程：写作 → /iterative-review 审到收敛 → 给我过目。
3. 过目通过后数据化进引擎（复用 chapter1 管道，lint 先行）。

红线：知识正确性（典籍照录+待核标注，编造出处是死罪）；美术卦纹只做装饰；
铜钱面背=3 勿回退；commit 随时 push 要我明说；只做以上范围，第二章《装卦》
等《钱囊》验证 bonus 形态后再开。
```
