# 天机卷项目路线图

更新日期：2026-07-09

## 当前结论

天机卷的统一产品由三个相互依赖、可以分阶段交付的部分组成：

- 实战工具层（Product A）：当前 repo 已实现的主要部分。
- 修仙学堂（Product B）：课程、测验、掌握度、灵力、境界和渡劫。
- 统一产品（Product C）：让教学与实战共享知识底座、进度和用户记录。

Database 是三者共同底座，不单独作为面向用户的产品。

## 阶段

### V0 数据库与排盘正确性验证（当前）

目标：在继续产品实现前，重新证明当前排盘引擎、共享历法、静态表和中医高风险内容是否可靠。

当前顺序：

- 八字四柱、节气、日界、真太阳时与大运。
- 六爻、梅花、紫微、奇门、风水等确定性盘面。
- 基础查表、解释性知识和中医安全数据。

完成条件：每个 runtime 数据消费点进入 validation ledger；八字取得独立复算结论；关键错误形成根因与修复批次；PRD 根据验证结果重新确认现有 Product A 的复用边界。

V0 期间暂停教学/游戏实现和新的横向功能扩展。

### P0 主线恢复与数据库基线（待评审）

目标：结束“资料散落、产品主旨不清、数据库状态不可见”的状态。

完成条件：

- `docs/PRD.md` 成为主产品定义。
- `docs/DATABASE_COVERAGE_MATRIX.md` 能看出每个领域有什么、缺什么、下一步补什么。
- `docs/PROMINENT_SOURCE_MANIFEST.md` 能追踪主要来源、版本关系、许可和校验状态。
- 用户确认第一条教学垂直切片。

### P1 教学核心可玩切片

目标：完成一个可玩的学习闭环，暂不铺满全部领域。

建议范围：阴阳五行与八卦基础，或六爻基础。包含课程、练习、规范答案、灵力、一次渡劫和进度保存。

完成条件：新用户能从进入课程到通过渡劫，系统能说明他学了什么、错在哪里、解锁了什么。

### P2 实战工具接入学习循环

目标：让现有 Product A 从独立工具变成课程后的实战场。

完成条件：至少一个排盘工具能根据已学知识生成练习任务、记录实战、触发复盘，并更新领域掌握度。

### P3 多领域课程与数据库扩展

目标：按覆盖矩阵逐域扩展，不按目录数量追求“大而全”。

优先顺序由三项共同决定：玩家价值、已有工具复用度、来源与校勘准备度。

### P4 叙事、NPC 与长期养成

目标：在学习循环成立后加入角色关系、成就、每日任务、剧情和可选道侣线。

完成条件：这些机制能提升继续学习和回来练习的动机。只增加文本和数值不算完成。

## 反漂移规则

- 新增一个排盘或问诊模块，不自动等于推进教学游戏。
- 原始资料导入，不自动等于数据库完成。
- AI 生成一套题，不自动等于教学内容可用。
- 境界、灵力和 NPC 只有在驱动学习行为时才算游戏进展。
- 中医方药、穴位和身体不适内容必须先过安全与来源门槛。

## 下一决策

八字确定性核心修复完成：项目已有 `lunar-javascript` 已替换手写节气、日期和起运 runtime，太阳时加入均时差；修复后 30 例 mismatch 清零，2,592 次节令侧检查和 39,447 日连续性通过。UI、账户、历史和通用交互继续保留。

第二独立历法实现与 fresh code review 已完成：`sxtwl@2.0.7` 的 30 个四柱样本全部一致，1920-2027 的 1,296 个节令时刻最大差 20 秒，四柱/节令达到 V3；reviewer 指出的陈旧 artifact 风险已用强制重建和源码 SHA256 关闭。天干五合、地支六合输出也已拆开“关系存在”和“有条件合化”。下一步审查大运第二证据、身强/用神、完整合化条件和神煞；历史民用时区/DST 作为共享时间数据单独验证。

在八字/共享历法处理结论与当前中医消费点安全 inventory 完成前，不启动 P1。之后才决定“基础通识 + 最小实战事件”或“六爻入门”作为首个可玩垂直切片。
