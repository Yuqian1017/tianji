# 交接文档 — 第三章交付后（2026-07-14）

> 写给下一个 session 的落听潮。本 session 完成：第三章《六亲》全链（审计→KP 卡→七幕剧本→三轮审查收敛含 P0 正典冲突重构→宗主过目→330 节点数据化→lint→四轮 E2E）+ UIUX 批次（可读性 P0 根修/NPC 立绘×6/对话框美术化）。读完再动手。

## 1. 三分钟状态全景

**能玩（三章+番外全线）**：`tianji-dev`（端口 5926）→ `game.html`。ch1《第一卦》→ ch2《装卦》→《钱囊》（ch2 通关+好感≥8 解锁）→ ch3《六亲》（ch2 通关解锁）。

**本批关键 commit**（全部本地未 push，`87efa71`→`fe6d8b8`+ 累计 ~98 个）：

| 层 | 内容 | 关键文件 |
|----|------|----------|
| ch3 审计 | 生克/六亲安法/主事三 KP，witness+引擎对撞（10/10·25/25·6/6） | `database/sources/ctext/liuqin-sanjian-2026-07-13.json` + `kp-ly-00{7,8,9}.json`（accepted） |
| ch3 剧本 | 8.5k 字七幕，v1.2 三轮收敛（R1 P0 正典冲突→甲案重构白芷下山线） | `docs/script/CHAPTER_3_SCRIPT_v1.md` |
| ch3 数据 | 330 节点；favorBranch（threshold 25）+ 双卦装亲盘（liuqin） | `src/game/chapters/chapter3.js` |
| lint | `npm run audit:chapter3`——fail 专属链好感排除 + liuqin↔paipan 对撞 | `scripts/validation/audit-chapter3-script.mjs` |
| 引擎 | favorBranch 路由 + DressingBoard 六亲列 + perThrow.speaker | `src/game/{Player,CastPanel}.jsx` |
| UIUX | `--g-*` 游戏视觉系统（P0：--color-surface 未定义→全透明根修）+ NPC 立绘×6 + 双人构图 | `src/game/game-ui.css` `presentation.js` |
| 资产 | bg-xiushufang/jishi/cangjinge-nei + 立绘 song/cui/baizhi/zheng/gu/han | `public/assets/game/` `scripts/generate-*.py` |

## 2. 已验证 vs 未验证（诚实清单）

**已验证**（本 session 运行时实测）：
- ch3 四轮 E2E：optimal 女版（111/30 精确·A×3·pass 链实走·明夷盘六亲渲染）/ 低好感 fail 链专测（23<25 分支正确）/ 师兄版（♂ 变体实渲染）/ 错线 B/C/B（96/25·三 KP 待复习·不锁进度）
- audit:chapter3 首跑全绿 330/330；eslint 0；vite build 0；console 零错误
- ch2 同标准全套（错线/师兄版）+ UIUX 改前改后截图对比

**仍未验证（下 session/玩测）**：
1. ch3 新 NPC 立绘/新背景**视觉细节**（状态断言全过，未逐屏截图审美检查；bg-cangjinge-nei 已入库未接线）
2. 真人玩测（终极验收「拿三枚硬币起一卦」，ch1 遗留至今）+ 三章时长实测
3. BGM 听感全程；装亲交互手感
4. 《钱囊》在 ch2 后新位置的运行时回归（门槛迁移后未重跑番外 E2E——GameModule 逻辑已改，数据未动，风险低但未实测）

## 3. 下一步待办（优先级）

1. **第四章预备**（主线）：钩子在案——山地剥应爻**巳火**的时辰/生肖指向（她收着不说）+ 白芷「怕的东西」+ 夜访者「在放什么」+ 残谱「点偏半分」藏秘 + 修书房两人（宋/崔）羁绊深化。KP 候选方向：用神旺衰初步？（判吉凶第一课——认知负荷大，可能要拆）——策划案先行，照抄本批管线。
2. 玩测批次（真人 + 时长 + 视觉审美 pass + 番外回归）
3. 表情差分 / 摇卦 SFX / 标题页文案陈旧（「天机·第一章」该改多章版）
4. 好感「信任」档解锁物：私教章①（六爻进阶选修，策划案 § 1.1）——设计案未写

## 4. 红线（全集，新增一条）

1-8. 同 HANDOFF_2026-07-13_ch2.md § 4（知识正确性/≤3 KP 闭环/铜钱面/剧本先行/commit-push/卦纹装饰/dressing 全量契约/MessageChannel walker）。
9. **新**：**交接文档不能替代正典回读**——写新章前必须重读前章剧本的相关段落（本批 P0 教训：凭 handoff 一句话自设修书房时间线，与 ch2 出纳册三重相撞）。涉及跨章事实（日期/人物动向/物证）时，grep 前章 doc 原文核对后再落笔。

## 5. 技术备忘（增量）

- favorBranch：静默路由，两链必须都汇到 chapterEnd；lint 的 fail 专属链好感排除语义见 audit-chapter3 § 5。
- DressingBoard liuqin：revealed 条目可选字段，显示「支五行·六亲」；board 仍全量幂等。
- ch3 act-5 节点 id 特例：集市锚 `ch3-s5-300` 固定（presentation 接线），锚前 id 步长 5。
- E2E walker 四轮模式全在本批 transcript 可抄（seed 构造 + CP_PICK 表 + 探针法）。

## 6. 交接 prompt（宗主直接贴给下个 session）

```
项目：天机（修仙恋爱教学游戏），路径 ~/CC projects/projects/games/tianji/

先读 docs/HANDOFF_2026-07-14_ch3.md，再按其指示行动。当前状态：
三章主线+番外全线可玩且 E2E 全绿（ch1→ch2→《钱囊》→ch3），
UIUX 视觉系统就位（NPC 立绘×6/对话框美术化），管道三轮验证成熟。

本 session 任务（择一，问宗主）：
A. 玩测批次：真人玩测陪跑 + 三章时长实测 + 视觉审美 pass + 番外回归
B. 第四章预备：策划案先行（KP 候选拆解 + 剧情钩子编排）→ 审计 → 剧本
红线照 HANDOFF § 4 全集（注意新增第 9 条：正典回读）。
```
