# 交接文档 — 第四章+私教章①收官，第五章预备（2026-07-17）

> 写给下一个 session 的落听潮。本 session 单日完成：跟随制审计第四批 → 第四章《旺衰》全链（剧本 994 行 + 四轮审查 + 数据化 328 节点 + E2E 四轮）→ 私教章①《演卦》全链（剧本 + 单轮审 + 数据化 95 节点 + E2E）。全部已 push。读完本文档再动手。

## 1. 三分钟状态全景

**能玩**：preview 配置 `tianji-dev`（端口 5926）→ `http://localhost:5926/game.html`。主线四章 + 两番外全线可玩：ch1《第一卦》→ ch2《装卦》→《钱囊》（ch2 通关+好感≥8）→ ch3《六亲》→ ch4《旺衰》→《演卦》（ch4 通关+好感≥35+natal 存在）。

**Push 状态**：全部同步 `origin/master`（宗主 2026-07-17「你自己审，自己往下推」授权链）。HEAD ≈ `5ba6494`。

**本 session 交付链**（`ff272be`→`5ba6494`，~24 commits）：

| 块 | 内容 |
|----|------|
| 审计第四批 | witness `wangshuai-sichu-2026-07-17.json`（11 passages 程序化摘录，提取行 545-593）+ `audit-chapter4-canon.mjs` 三层对撞 85 checks（保真/引擎 24+2+8/KP 链）+ KP-LY-010/011/012（accepted） |
| ch4 剧本 | 994 行七幕 + 四轮审查（R1 opus 全量 0P0 / R2 opus 抓代词回归 58 处 / R3 delta 收敛 / R4 教学引导性——宗主指令「不要写得太生涩」3 修润）+ 过目通过（委托）。逐字引文 14 + 跨章正典 14 全部程序化对撞 |
| ch4 数据化 | 双 agent 转录合并 328 节点；`audit:chapter4` lint 全绿（新四机检：禁词/称谓中性/裸她白名单/wangshuai 限位）；registry+GameModule；CastPanel wangshuai 显示位；**fixedDate 契约评估=零改动**（fixed 模式不走运行时排盘） |
| ch4 E2E | 四轮全绿：optimal 女 148/40（pass 链实走）/ fail 链（19<25 实判 fail010-030）/ 错线 B·C·B 133/35（待复习不锁）/ 男版 148/40（「多谢师兄」probe、零代词泄漏）。console 零错误；wangshuai 视觉截图（初爻「巳火·父母·旺」） |
| 私教章① | 《演卦》255 行五幕 → 单轮审 5 findings 全折修 → 95 节点 + `natalDressingBoard(natal, stage)` 三档动态盘 helper（对撞 paipan 全 MATCH）+ Player `dynamicNatalBoard` 分支 → E2E 153/47 精确、动态盘视觉过审（水泽节替换渲染） |

## 2. 已验证 vs 未验证（诚实清单）

**已验证**（全部运行时实测，console 零错误）：
- ch4 E2E 四轮 + sijiao1 E2E optimal（数值/分支/探针/视觉全绿，见 CHANGELOG 2026-07-17 条目）
- `audit:chapter4-canon` 85 checks / `audit:chapter4` lint 0 fail 0 warn / eslint 0 / vite build ✓
- 动态盘 helper node 层三档对撞 + null fail-loud；「私教解锁」gate 三条件亮灯逻辑

**仍未验证（下 session/玩测）**：
1. **真人玩测**（PRD 终极验收——四章+两番外全欠，积欠三个 session 了）
2. **重温模式重复计分**（本 session 新观察：重温 ch4 时灵力/好感再累积——Player 层跨章既有行为非 ch4 引入；真人玩测批一并裁定是否修）
3. BGM 实际听感；各章时长实测（ch4 纸面 31-46 / 演卦 15-25 均未实测）
4. 立绘表情差分（全员单表情）；摇卦 SFX；标题页「天机·第一章」文案陈旧（陈了三个 session）
5. sijiao1 的 fail 分支不存在（单线无 favorBranch——设计如此，玩测确认体感）

## 3. 下一步待办（优先级）

1. **真人玩测批次**（强烈建议本批做——四章两番外全线欠玩测，时长/难度/情感曲线全是纸面值；宗主上手 + 校准 + 表情差分需求收集）。
2. **第五章《元神忌神》预备**（若宗主要继续推主线）：witness 950329 提取行 773 起「元神忌神衰旺章」**已在库**（audit 同口径可直接提取）；剧情钩子存量充足（§ 4 正典钉子清单）——先策划案（照 CHAPTER_4_PREP 模板）过目再动。
3. 美术打磨批（表情差分/SFX/标题页文案）。

## 4. 红线（全集 11 条——第 2 条白名单已更新）

1. 知识正确性：accepted KP 才进剧情；原文照录+定位+witness；编造出处死罪。
2. 每章 ≤3 新 KP、示范→引导→独立闭环；未教不考（**ch4 已解禁：月建/日辰/旺相/休囚/月破/暗動/當令；仍禁：旬空/空亡/伏神/元神/忌神（ch5 天然候选）/六冲/六合/三合/三刑**——lint BANNED_TERMS 即白名单落实）。
3. 铜钱面：背=3（三背=重=老阳）勿回退。
4. 剧本先行：数据改动先改剧本 doc（本 session 两次实践：崔小砚 {{senior}} 修复、演卦审查修复均先 doc 后 data）。
5. commit 随时、push 分段推（§ 5）；`.claude/launch.json` 既有改动勿动。
6. 美术卦纹不作教学参照；教学卦象一律代码绘制。
7. dressingUpdate board 全量幂等契约勿改增量；favorBranch pass/fail 必须双链收敛 chapterEnd。**新增：dynamicNatalBoard 动态盘同守幂等契约（helper 每 stage 输出全量 board）**。
8. E2E walker 必须 MessageChannel 泵；**新增：walker 支持 `cpPicks` 序列（错线轮选非首项，本 session 加的）**。
9. 正典回读：跨章事实不凭 handoff 摘要、不凭记忆——写前回读前章剧本原文。
10. 引用必 grep：任何标「逐字」的引用写完必程序化对撞——**含跨章正典句**（本 session ch4 剧本 14+14 双层对撞先例）。
11. **新增（本 session 教训）：脚本工具必须 assert 验证写入**——CHANGELOG 三次静默 no-op（replace 锚不匹配仍打印成功）事故；任何 python replace 后必须 assert 结果存在再报成功。

## 5. 技术备忘（坑与契约）

- **新立正典钉子（ch5 沿用勿漂移）**：占期=卯月辛巳日（引擎参考输入 2026-03-08）；生肖档案：宋酉鸡/崔子鼠/白未羊/郑辰龙（皆非蛇→指向名册之外）；崔三遍=腊月初二/初三/初五（证词失令不失真，嫌疑降级不出局）；「点偏半分」页=风地观（乾宫第五，世点上偏指五爻辛巳官鬼；与山地剥谱上相邻）；窗台样张=泛黄练点纸（记号实锤，编码/谁放/给谁看全悬）；ch4 章末=守窗轮值开启（玩家巳时/她夜里）；ch4 案卦=地泽临（父母巳火用神旺·月生日临→「他会来」）。
- **悬置钩子清单（第五章素材）**：①样张编码何意/给谁看 ②「谁」（应爻巳火官鬼的人身）③白芷所惧实底 ④「我认得这个起笔」（她收着的师承层）⑤属蛇指向名册之外（第四人？）⑥守窗行动线（「等他自己走到卦里来」→「临者至也」已应）。
- **Push**：本 session 14+10 commits 两次直推成功（未触发断包）；大包仍备分段法（`git rev-list --reverse origin/master..HEAD | awk 'NR % 15 == 0'` 逐段推）。
- 转录管线：双 agent 幕段拆分+固定锚 id（本轮 `ch4-s5-header`）一次合并全绿——第五轮了，agent spec 要点照抄 handoff 2026-07-14 § 5；**新增要点：spawn 需带 6 防御 patterns block（pre-agent hook 强制）**；B agent 的裁量上报机制价值实证（抓到剧本层 {{senior}} bug + 两段 dressingUpdate 防泄题设计）。
- witness 提取口径：去 script/style + 标签转换行 + strip + 去空行 + 去首行 title 的 1-based 行号（`/tmp/ctext-950329.txt` 可随时重生成；`audit-chapter4-canon.mjs` 内置同款提取函数可复用）。
- 动态本命卦设施：`natalDressingBoard(natal, stage)`（state.js）+ 节点 `dynamicNatalBoard: 'branches'|'marks'|'full'` + `dynamicNatal`（ch2 6.4 全角〔〕占位符）——ch5 若做本命卦相关内容直接复用。
- E2E 种子：ch4 通关态 = lingli 148 / favor 40（optimal）；sijiao1 通关态 = 153/47；natal 测试卦 [9,7,8,8,7,8]=水泽节坎宫一世（带动爻，动爻圈渲染正常）。

## 6. 交接 prompt（宗主直接贴给下个 session）

```
项目：天机（修仙恋爱教学游戏，Galgame 形态），路径 ~/CC projects/projects/games/tianji/

先读 docs/HANDOFF_2026-07-17_ch5.md（交接：状态/红线11条/正典钉子/技术契约/待办），
再按其指示行动。当前状态：主线四章 + 番外《钱囊》+ 私教《演卦》全线可玩且
E2E 全绿（ch4 四轮矩阵 + 演卦动态盘视觉过审），全部已 push。

本 session 任务（按优先级）：
1. 真人玩测批次（四章两番外全欠——时长/难度/情感曲线校准 + 重温重复计分裁定
   + 表情差分需求收集）
2. 若推主线：第五章《元神忌神》先策划案（witness 提取行 773 起已在库，
   照 CHAPTER_4_PREP.md 模板做 KP 拆解+钩子编排+正典锚点，过目后再审计→剧本）
3. 美术打磨批（表情差分/SFX/标题页文案）

红线照交接文档 § 4 全集 11 条执行（新增 #11 脚本工具 assert 验证——本 session
CHANGELOG 三次静默 no-op 换来的）。commit 随时，push 分段（§ 5）。
```
